services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./omni_wells:/app/omni_wells
    environment:
      OMNI_WELLS_NO_BROWSER: "1"
    command: python -c "from omni_wells.app import app; app.run(host='0.0.0.0', port=5000, debug=True, use_reloader=False)"